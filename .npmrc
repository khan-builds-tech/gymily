# React Native / Metro can't resolve pnpm's default symlinked, isolated
# node_modules. Expo's monorepo guide requires a hoisted (flat) layout.
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
