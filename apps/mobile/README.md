# apps/mobile

Gymily mobile app — **React Native + Expo + TypeScript** (Expo Router, TanStack Query).

> Not yet initialized. Next setup step:
>
> ```bash
> pnpm create expo-app@latest apps/mobile --template
> ```
>
> Then wire the Supabase client, Mapbox, and the auth gate per `docs/phase-1-architecture.md` and `docs/phase-2-flow.md`.
